"use client";

/**
 * Stub — locale picker removed (single-locale build, fr only). Kept as
 * a no-op component so consumers don't need to be edited at import
 * sites. If a multi-locale build is ever brought back, restore from
 * git history.
 */
export function LocaleSwitcher() {
  return null;
}
